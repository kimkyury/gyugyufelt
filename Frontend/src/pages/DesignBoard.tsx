import React from 'react'
import { DesignWritePage } from '@features/designBoard/pages/DesignWritePage'

export default function Masking() {
    return (
        /*(<main className="min-h-screen flex flex-col items-center justify-center p-8">
          */
        <main>
            <DesignWritePage />
        </main >
    );
}